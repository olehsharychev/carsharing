doctype html
html
	head
		link(rel="stylesheet", type="text/css", href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css")
		link(rel="stylesheet", href="/stylesheets/style.css")
	body
		header
			div.navbar
				a(href="/cars") На главную
				- if (currentUser === 0)
					a(href="/login") Вход
					a(href="/registration") Регистрация
				- if (currentRole === 3)
					a(href="/create-ad") Создать объявление
				- if (currentUser !== 0)
					a(href="/chats")= "Сообщения (" + amountUnread + ")"
					a(href="/profile/#{currentUser}") Мой профиль
					a(href="/logout") Выйти
		block content